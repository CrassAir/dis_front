import React from 'react';

type EditableFieldProps = {
    title: string
    value: string
    edit: boolean
}

const EditableField = ({title, value, edit = false}: EditableFieldProps) => {

    return (
        <div>

        </div>
    );
};

export default EditableField;